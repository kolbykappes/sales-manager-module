export const environment = {
  production: false,
  apiUrl: 'http://localhost:8000/api/v1'
};

// If you're using HTTPS or a different port, adjust accordingly:
// apiUrl: 'https://localhost:8000/api/v1'
// or
// apiUrl: 'http://localhost:8000'  // If the '/api/v1' part is handled by the backend routing
