export const environment = {
  production: false,
  apiUrl: 'http://localhost:8000/api/v1',  // Verify this URL
  withCredentials: true
};

// If your backend is running on a different port or host, adjust the apiUrl accordingly
// For example:
// apiUrl: 'http://localhost:5000/api/v1'
